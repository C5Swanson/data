import PropTypes from "prop-types";
import "./Post.scss"

const propTypes = {
    /*
    STEP 1

    TODO: Configure props

    Requirements:
    - Either a post object containing the required information or individiual props

    */
}

const Post = function({ users }) {

    return (
        <>
            {
                /*
                STEP 1
                
                TODO: Display post

                Requrirements:
                - Post needs to be in an elevated card
                - Post needs to display a title
                - Post needs to display a body message
                - Post needs to display users first and last name
                - Post needs to display post time in a user friendly format

                */
            }
        </>
    )
}

Post.propTypes = propTypes;

export default Post;